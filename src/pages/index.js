// Import the HomePage component from the 'views' directory
import HomePage from '@/views/HomePage';
import React from 'react';

// Define the Home component
export default function Home() {
	// Render the HomePage component
	return <HomePage />;
}
